import Page from "@reason/pages/Docs";
import DocLink from "@reason/components/DocLink";
export default Page({ title: "Mina Client SDK Keypair" });


# Клиентский SDK 

Клиентский SDK для NodeJS, который позволяет генерировать пары ключей Mina и подписывать с их помощью транзакции и строки. Проект содержит типизацию Typescript и ReasonML, но может использоваться и из простой NodeJS.


<br />

<Alert kind="warning">

Создание keypair с помощью этого метода не рекомендуется и должно использоваться только опытными специалистами в области безопасности.
Для создания keypair используйте наш <DocLink copy="генератор-ключей-mina" url="/using-mina/keypair#mina-generate-keypair" />.

Никогда не используйте повторно один и тот же keypair из тестовой сети в mainnet!
</Alert>


## Установка

Открытый код для нашего клиентского SDK можно найти [здесь](https://github.com/MinaProtocol/mina/tree/develop/frontend/client_sdk).

Чтобы установить клиентский SDK, вам понадобится NodeJS. Убедитесь, что она доступна на вашем устройстве, а затем используйте yarn или npm для установки клиентского SDK. 

```bash
# yarn:
yarn add @o1labs/client-sdk

# npm:
npm install --save @o1labs/client-sdk
```

## Использование

Создать keypair можно просто с помощью нашего клиентского SDK. Смотрите фрагмент ниже, чтобы узнать, как это сделать.

### NodeJS:
```javascript
const MinaSDK = require("@o1labs/client-sdk");

let keys = MinaSDK.genKeys();
console.log(`Public key: ${keys.publicKey}`)
console.log(`Private key: ${keys.privateKey}`)
```
Этот метод выведет ваш закрытый ключ, поэтому убедитесь, что вы находитесь в безопасной обстановке при его использовании.

Ознакомьтесь с [материалами по клиентскому SDK](https://github.com/MinaProtocol/mina/blob/develop/frontend/client_sdk/README.md) для более детальной информации по использованию SDK.

<br />

<Alert kind="warning">

Никогда не делитесь своим закрытым ключом и убедитесь в надежности его хранения. Если вы потеряете свой секретный ключ, или злоумышленник получит к нему доступ, вы потеряете доступ к своей учетной записи и средствам на ней. Вместо этого всегда делитесь только своими открытыми ключами. Mina никогда не запрашивает у вас ваши закрытые ключи.

</Alert>

<br />

## Импортируйте свой аккаунт из SDK

Обратите внимание, что существуют различия в формате keypair между тем, что ожидает клиент Mina, и тем, что генерирует клиентский SDK.
При использовании клиентского SDK для генерации keypair выходные данные представляют собой строки, которые нельзя напрямую импортировать в наш клиент Mina.
Для этого нам придется использовать инструмент `mina advanced wrap-key` для перевода строк нашего keypair в формат, который напрямую импортируется в наш клиент.

Для этого потребуется выполнить всего несколько простых шагов. 

1. Во-первых, убедитесь, что в вашей системе есть папка, где вы можете хранить файлы с ключами. Мы рекомендуем использовать папку ~/keys.

```
mkdir ~/keys
```

2. Затем убедитесь в правильности установленных в папке разрешений, это предотвратит доступ нежелательных процессов к вашим файлам.

```
chmod 700 ~/keys
```

3. Скопируйте закрытый ключ в файл в папке ~/keys.

```
echo 'YOUR-PRIVATE_KEY' > ~/keys/my-wallet
```
4. Убедитесь, что разрешения для файла с закрытым ключом установлены правильно, это предотвратит доступ к нему нежелательных процессов.

```
chmod 600 ~/keys/my-wallet
```

5. Используйте инструмент `mina advanced wrap-key`, чтобы перевести вашу пару ключей в формат, который может импортироваться клиентом Mina. Обратите внимание, что использование `mina advanced wrap-key` заставит вас сбросить пароль на вашем закрытом ключе.

```
mina advanced wrap-key --privkey-path ~/keys/my-wallet
```

Вам будет предложено ввести новый пароль, как показано ниже.

```
Private key:
Password for new private key file: 
Again to confirm:
```

Это сгенерирует новые файлы `my-wallet.pub` и `my-wallet`.

```
ls ~/keys/
my-wallet  my-wallet.pub
```

6. Убедитесь, что вы правильно установили разрешения для вновь созданного файла с закрытым ключом. 

```
chmod 600 ~/keys/my-wallet
```

7. Наконец, мы можем импортировать учетную запись, созданную клиентским SDK, в клиент Mina.

```
mina accounts import --privkey-path ~/keys/my-wallet
```

Вам будет предложено ввести пароль, который вы указали при создании учетной записи с помощью `mina advanced wrap-key`.

Ответ после ввода этой команды будет выглядеть так: 
```
😄 Imported account!
Public key: B62qjaA4N9843FKM5FZk1HmeuDiojG42cbCDyZeUDQVjycULte9PFkC
```


## Подтвердите свой закрытый ключ

После создания своего ключа нужно проверить, что он работает. Достаточно убедиться, что вы можете подписать транзакцию. Вы можете проверить это с помощью инструмента `mina-validate-keypair`. 

Запустите следующую команду:

```
mina-validate-keypair --privkey-path ~/keys/my-wallet
```
