import Page from "@reason/pages/Docs";
import DocLink from "@reason/components/DocLink";
export default Page({
  title: "Github | Mina Protocol",
  description:
    "Mina Protocol is open-sourced. Access the code behind Mina Protocol.",
});

# Github
Access the code behind Mina Protocol